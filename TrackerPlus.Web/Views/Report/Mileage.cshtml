@{
    Layout = null;
    var today = ViewBag.Today as string ?? DateTime.Today.ToString("yyyy-MM-dd");
}
<form data-report-download="/Report/DownloadMileage">
    <input type="hidden" name="imei" value="" />

    <div class="mb-2 row">
        <label class="col-sm-4 col-form-label fw-semibold">@L["Report_StartDate"]</label>
        <div class="col-sm-8">
            <input type="date" class="form-control form-control-sm" name="date" value="@today" required />
            <div class="form-text" style="font-size:11px;">@L["Report_MileageNote"]</div>
        </div>
    </div>

    <div class="mb-2 row">
        <label class="col-sm-4 col-form-label fw-semibold">@L["Report_GpsStatus"]</label>
        <div class="col-sm-8 d-flex flex-column gap-1 justify-content-center">
            <div class="form-check mb-0">
                <input class="form-check-input" type="radio" name="gpsFilter" value="all" id="mi-gps-all" checked />
                <label class="form-check-label" for="mi-gps-all">@L["Report_GpsFilterAll"]</label>
            </div>
            <div class="form-check mb-0">
                <input class="form-check-input" type="radio" name="gpsFilter" value="gps" id="mi-gps-only" />
                <label class="form-check-label" for="mi-gps-only">@L["Report_GpsFilterGps"]</label>
            </div>
            <div class="form-check mb-0">
                <input class="form-check-input" type="radio" name="gpsFilter" value="nogps" id="mi-gps-no" />
                <label class="form-check-label" for="mi-gps-no">@L["Report_GpsFilterNoGps"]</label>
            </div>
        </div>
    </div>

    <div class="mb-3 row">
        <label class="col-sm-4 col-form-label fw-semibold">@L["Report_OutputMode"]</label>
        <div class="col-sm-8">
            <select name="format" class="form-select form-select-sm">
                <option value="csv">@L["Report_OutputCsv"]</option>
                <option value="xls">@L["Report_OutputXls"]</option>
            </select>
        </div>
    </div>

    <div class="d-flex justify-content-end gap-2">
        <button type="reset" class="btn btn-outline-secondary btn-sm">@L["Common_Reset"]</button>
        <button type="submit" class="btn btn-primary btn-sm">@L["Common_Ok"]</button>
    </div>
</form>
